{
  "name": "legi-detect",
  "version": "1.0.0",
  "private": true,
  "license": "Apache-2.0",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "lerna": "^3.16.4"
  },
  "scripts": {
    "build": "lerna run build",
    "dev": "lerna run dev",
    "lint": "lerna run lint",
    "start": "lerna run start",
    "test": "lerna run test"
  }
}
